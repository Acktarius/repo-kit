# Package preferences

Apply this repository's Node/TypeScript package and tooling preferences.

## Instructions

1. Read `.cursor/skills/package-preferences/SKILL.md` and follow it.
2. Prefer Biome for format/lint when present.
3. Keep `package.json` scripts aligned with CI (`lint`, `typecheck`, `test` as applicable).
4. Do not invent a different formatter or package manager policy without updating `.npmrc` and docs.
