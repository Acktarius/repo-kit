# Rust setup

Apply Rust project scaffolding and style guidance.

## Instructions

1. Read `.cursor/skills/rust-standards/SKILL.md` and follow it.
2. Prefer `rustfmt` via `rustfmt.toml` and standard Cargo workflows.
3. Keep `cargo fmt`, `cargo clippy`, and `cargo test` green when changing code.
